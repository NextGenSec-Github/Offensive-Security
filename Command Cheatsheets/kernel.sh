# Kernel Commands

sudo insmod kern.ko # Insert a kernel module into the Linux kernel with insmod (Insert Module)
sudo lsmod # Check whether the module has loaded
dmesg # Display the kernel ring buffer messages
sudo rmmod file # Removing the kernel module 
sudo dmesg -c # Clear the kernel buffer 






